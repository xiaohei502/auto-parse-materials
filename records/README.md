# Records

长期存档区。这里保存资料依据，不只保存最终总结。

建议保留四层：

- `originals/`：原文记录，尽量保存用户粘贴或收集到的原始文本。
- `transcripts/`：转写稿、字幕稿、逐字稿、口播稿。
- `preprocessed/`：预处理后的清洗稿。
- `metadata/`：来源、日期、标签、处理状态等元信息。

最终总结和二次产出仍保存到 `outputs/` 或 `artifacts/`。
